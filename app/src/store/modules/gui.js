const state = {
    formState: true
};
const getters = {
    isForm: state => state.formState
};
const actions = {
    setForm: function({commit}) {

    }
};
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};
